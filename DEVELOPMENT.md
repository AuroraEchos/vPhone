# vPhone 开发规范与项目推进指南

本文档用于帮助项目维护者理解一个真实软件项目如何从想法逐步推进到可演示、可维护、可发布的产品。

它既是 vPhone 的开发约定，也是一份个人学习手册。当前项目仍处于 `dev` 阶段，规范的目的不是增加流程负担，而是让每一次改动都清晰、可验证、可回退。

## 1. 项目当前阶段

vPhone 当前处于早期开发阶段：

- 暂不发布 PyPI 包或 GitHub Release。
- 版本使用开发版本号，例如 `0.1.0.dev0`。
- 优先完成核心架构和第一个可运行 Demo。
- 使用真实 Android 设备进行集成验证。
- 使用 `uv` 管理 Python、依赖和虚拟环境。
- `main` 应逐渐成为经过验证的稳定开发主线。

当前规划的五层架构：

```text
L5 Interface   CLI / Python API / 可选 Web UI
        ↓
L4 Planner     任务理解、状态维护、动作决策
        ↓
L3 Perception  控件树与视觉混合感知
        ↓
L2 Action      动作校验、执行、结果表达
        ↓
L1 Device      设备连接、原始观察和输入原语
```

依赖方向原则上只能从上层指向下层。下层不能导入或了解上层业务。

## 2. 开发的核心原则

### 2.1 先明确问题，再写代码

每项开发开始前，至少回答：

1. 要解决什么问题？
2. 为什么现在需要解决？
3. 哪一层负责？
4. 对外接口是什么？
5. 如何证明它完成了？
6. 哪些内容明确不在本次范围内？

如果这些问题无法回答，说明任务还不能直接进入编码阶段。

### 2.2 每次只解决一个明确问题

一个分支、Pull Request 或主要提交应围绕一个主题，例如：

```text
实现 ADB 设备发现
增加截图采集
修复控件树并发竞争
新增点击动作校验
更新 L1 架构文档
```

不要在实现一个功能时顺便重命名大量文件、格式化整个项目、升级所有依赖和修改无关文档。

### 2.3 先保证正确，再考虑抽象

新项目最常见的问题之一是过早设计复杂框架。

vPhone 当前遵循：

- 没有第二种实现时，不急于建立插件系统。
- 没有真实并发需求时，不引入异步体系。
- 没有重复代码造成维护问题时，不强行抽象。
- 不为尚未出现的需求增加复杂配置。
- 发现真实问题后，再用最小改动解决。

### 2.4 所有成功都必须可以验证

“代码写完了”不等于任务完成。

至少需要：

- 单元测试通过。
- 静态检查通过。
- 与真实设备相关的功能经过真机验证。
- 文档和代码行为一致。
- 没有混入无关文件、密钥或本机产物。

## 3. 一个功能的完整生命周期

真实项目中的功能通常经历以下流程：

```text
提出需求
→ 明确范围和验收条件
→ 讨论设计与接口
→ 拆分任务
→ 创建分支
→ 编写代码和测试
→ 本地验证
→ 推送分支
→ 创建 Pull Request
→ Code Review
→ 修正问题并重新验证
→ 合并 main
→ 删除已完成分支
→ 更新下一阶段计划
```

### 3.1 需求描述

一个合格的需求应包含：

```text
背景：为什么需要这个功能。
目标：完成后用户或系统能做什么。
范围：本次包含什么。
非范围：本次明确不做什么。
验收条件：怎样判断已经完成。
风险：可能影响哪些模块或数据。
```

例如：

```text
目标：设备层能够获取当前屏幕控件树。
范围：ADB + UI Automator XML，解析为 RawUiNode。
非范围：不做元素语义匹配，不做视觉融合。
验收条件：离线 XML 测试通过，真机能够返回节点快照。
风险：UI Automator 并发调用可能冲突，敏感节点需要脱敏。
```

### 3.2 设计讨论

编码前优先确定：

- 数据模型。
- 模块职责。
- 公共接口。
- 错误类型。
- 同步或异步模型。
- 是否需要第三方依赖。
- 测试策略。

接口一旦被上层使用，修改成本会迅速增加，因此接口设计比内部实现更值得提前讨论。

### 3.3 任务拆分

一个大型层级不要作为一个无限扩张的任务。可以拆成：

```text
L2 Action
├── 动作数据模型
├── 参数验证
├── 点击执行
├── 滑动执行
├── 文本输入
├── 执行结果
├── 错误分类
└── 真机集成测试
```

每个子任务都应该能够独立测试和审核。

## 4. Git 分支规范

### 4.1 main 分支

`main` 是项目的可信主线，应满足：

- 已合并的代码经过审核。
- 自动化测试通过。
- 不包含已知的阻断性错误。
- 可以作为新功能分支的起点。
- 禁止随意强制推送。

当前是个人开发，也应尽量保持这一习惯。

### 4.2 功能分支

建议的分支名称：

```text
feature/l1-device
feature/l2-action
feature/hybrid-perception
fix/adb-timeout
fix/tree-capture-race
refactor/device-errors
docs/development-guide
chore/update-dependencies
review/l2-action-pr
```

分支前缀含义：

- `feature/`：新功能。
- `fix/`：错误修复。
- `refactor/`：不改变外部行为的代码重构。
- `docs/`：纯文档修改。
- `test/`：测试相关修改。
- `chore/`：依赖、工具或工程配置。
- `review/`：包含代码审核、测试以及必要的修正，通常不直接合并。

### 4.3 创建分支

```bash
git switch main
git pull --ff-only
git switch -c feature/l2-action
```

新分支应从最新且测试通过的 `main` 创建。

### 4.4 分支生命周期

功能分支应尽量短：

- 理想情况为几小时到几天。
- 不要让一个分支长期承载多个无关目标。
- 功能完成后及时创建 Pull Request。
- 合并后删除远程和本地分支。

长期分支更容易与 `main` 产生大量冲突。

## 5. 两台电脑之间的日常工作流

GitHub 是两台电脑之间的代码协调中心，但只能同步已经提交并推送的内容。

### 5.1 开始工作

无论在实验室电脑还是笔记本，首先执行：

```bash
cd vPhone
git status
git switch main
git pull --ff-only
uv sync --extra dev
```

如果要继续某个功能分支：

```bash
git fetch origin
git switch feature/l2-action
git pull --ff-only
```

### 5.2 开发过程中

经常查看：

```bash
git status
git diff
```

运行验证：

```bash
uv run pytest
uv run ruff check .
uv run ruff format --check .
```

### 5.3 结束工作

离开当前电脑前：

```bash
git status
git diff
git add -p
git commit -m "feat(action): add validated tap action"
git push
```

不要把唯一一份重要修改长期留在某台电脑的未提交工作区中。

### 5.4 有未提交修改时不要盲目 pull

先运行：

```bash
git status
```

如果本地修改已经有意义，应先提交。如果只是短暂实验，可以使用：

```bash
git stash push -m "temporary experiment"
git pull --ff-only
git stash pop
```

`stash` 只适合短期停放，不应作为备份或跨电脑同步方案。

## 6. Commit 规范

### 6.1 一个 Commit 应该是什么

一个好的 Commit 应当：

- 只表达一个完整意图。
- 能够独立理解。
- 尽可能通过测试。
- 不包含密钥和构建产物。
- 必要时可以被单独回退。

### 6.2 Commit 消息格式

建议使用：

```text
<type>(<scope>): <summary>
```

常见类型：

```text
feat      新功能
fix       错误修复
refactor  重构
test      测试
docs      文档
chore     工程维护
perf      性能优化
```

示例：

```text
feat(device): add adb screenshot capture
fix(device): serialize tree capture per device
test(device): cover unauthorized adb state
docs: add project development guide
```

避免模糊信息：

```text
update
fix bug
change files
work in progress
```

### 6.3 使用 git add -p

推荐：

```bash
git add -p
```

它允许逐段检查即将提交的代码，可以避免：

- 提交调试输出。
- 提交无关格式化。
- 提交本机配置。
- 把两个不同目标混进一个 Commit。

## 7. Pull Request 规范

即使个人开发，重要功能也建议通过 Pull Request 合并。

### 7.1 PR 描述应包含

```text
背景
本次改动
明确未做的内容
关键设计决定
测试方式与结果
真机验证结果
已知限制
审核重点
```

### 7.2 PR 不应过大

如果审核者需要一次理解几十个无关概念，PR 就过大了。

应该拆分为：

```text
PR 1：数据模型和接口
PR 2：ADB 实现
PR 3：控件树采集
PR 4：动作执行
```

但也不要为了追求极小 PR，把一个不可运行的完整功能拆得支离破碎。每个 PR 应形成可以验证的进展。

### 7.3 Code Review 顺序

建议按照以下顺序审核：

1. 需求和范围是否正确。
2. 模块职责是否正确。
3. 公共接口是否合理。
4. 正常流程是否正确。
5. 错误和边界情况是否处理。
6. 测试是否真正覆盖行为。
7. 命名、格式和注释是否清晰。

不要一开始只检查空格和命名，却忽略整体设计方向。

## 8. 冲突处理规范

代码冲突是正常现象，但应尽早暴露并控制范围。

### 8.1 降低冲突的方法

- 使用短生命周期分支。
- 经常同步 `main`。
- 不在功能提交中格式化整个仓库。
- 公共接口修改前提前沟通。
- 避免多个成员同时进行大规模重命名。
- 按清晰模块拆分代码。

### 8.2 同步 main

初学阶段推荐在功能分支中合并最新主线：

```bash
git fetch origin
git merge origin/main
```

发生冲突后：

```bash
git status
```

理解双方修改目的，编辑冲突文件，然后：

```bash
git add <resolved-files>
git commit
uv run pytest
git push
```

解决冲突后必须重新测试。Git 能确认文本已经合并，但不能确认业务逻辑正确。

### 8.3 uv.lock 冲突

不要手工拼接复杂锁文件。通常应：

1. 先解决 `pyproject.toml`。
2. 确认双方依赖都正确保留。
3. 重新生成锁文件。

```bash
uv lock
uv sync --extra dev
uv run pytest
```

## 9. 架构与代码边界

### 9.1 按层负责

以 vPhone 为例：

- Device 不做元素语义判断。
- Action 不负责理解用户任务。
- Perception 不执行设备命令。
- Planner 不直接调用 ADB。
- Interface 不复制核心业务逻辑。

如果一个模块需要绕过其他层才能工作，应先讨论架构，而不是直接增加快捷路径。

### 9.2 公共接口优先稳定

公共接口包括：

- 数据模型。
- Protocol 或抽象接口。
- 公开异常。
- 对外导出的函数和类。
- 配置名称。

修改公共接口时需要同步：

- 调用者。
- 测试。
- 文档。
- 迁移说明。

### 9.3 错误必须有语义

不要让上层依赖底层字符串：

```python
if "unauthorized" in str(error):
    ...
```

应在边界层转换为稳定异常：

```python
except DeviceUnauthorizedError:
    ...
```

### 9.4 避免隐藏副作用

函数名称和接口应能反映副作用。

例如设备层不应在一次普通失败后自动：

- 重启 ADB Server。
- 重新点击。
- 安装 APK。
- 切换输入法。
- 清除应用数据。

这些动作必须由明确的上层策略或用户请求触发。

## 10. 测试规范

### 10.1 测试层级

vPhone 使用三类测试：

```text
单元测试
验证纯函数、解析器、校验和错误分类，不需要手机。

集成测试
验证 ADB 和真实设备能力，需要显式设备 ID。

端到端测试
验证完整 Agent 是否完成真实任务，后续 Demo 阶段建立。
```

### 10.2 默认测试不能依赖真机

普通测试应可以在没有 Android 设备的电脑和 CI 中执行：

```bash
uv run pytest -m "not android"
```

真机测试显式运行：

```bash
VPHONE_DEVICE_ID=<serial> uv run pytest -m android
```

### 10.3 修复 Bug 时先增加回归测试

推荐流程：

1. 用最小场景复现 Bug。
2. 增加一个能够失败的测试。
3. 修复代码。
4. 确认新测试和旧测试全部通过。
5. 记录根因，而不仅是表面现象。

真实例子：L1 真机并发测试发现多个 Session 同时抓取控件树时失败。修复不是增加无条件重试，而是确认 UI Automator 需要同设备串行，然后增加共享锁和回归测试。

### 10.4 测试应验证行为

不要只追求覆盖率数字。测试应该验证：

- 输入和输出。
- 状态变化。
- 错误类型。
- 边界值。
- 并发行为。
- 副作用是否发生。

## 11. 代码质量工具

日常检查：

```bash
uv run pytest
uv run ruff check .
uv run ruff format --check .
```

开发时建议在工作区开启 Pylance：

```json
{
  "python.analysis.typeCheckingMode": "standard"
}
```

当前先使用 `Standard`。公共接口稳定后，再评估 `Strict` 和命令行 Pyright 检查。

不要为了让工具“变绿”而随意忽略错误。应先判断是代码问题、类型声明问题，还是规则不适合项目。

## 12. uv 与依赖管理

### 12.1 依赖来源

- 运行时依赖写入 `[project].dependencies`。
- 测试和开发工具写入 `[project.optional-dependencies].dev`。
- 不依赖某台电脑全局安装的 Python 包。

### 12.2 修改依赖

修改依赖后：

```bash
uv lock
uv sync --extra dev
uv run pytest
```

`pyproject.toml` 和 `uv.lock` 应一起提交。

### 12.3 不提交本地环境

禁止提交：

```text
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/
```

其他电脑使用 `uv sync --extra dev` 重建环境。

## 13. 配置与敏感信息

禁止提交：

- API Key。
- 密码。
- 私钥。
- Access Token。
- 个人设备凭证。
- 包含真实敏感数据的截图或控件树。

推荐方式：

```text
.env          本地真实配置，不提交
.env.example  配置示例，可以提交
```

程序日志默认不应记录密码输入、完整 Token 或密码控件内容。

如果密钥曾经进入 Git 历史，仅删除文件不够，还需要立即撤销并更换密钥。

## 14. 文档规范

文档不是开发结束后的装饰，而是项目接口的一部分。

需要文档化的内容：

- 架构职责。
- 公共 API。
- 环境准备。
- 测试方法。
- 已知限制。
- 重要设计决定。
- 发布和迁移步骤。

代码变化导致文档失真时，应在同一个 PR 中更新文档。

大型设计决定可以使用 ADR：

```text
docs/adr/0001-use-adb-backend.md
docs/adr/0002-hybrid-perception.md
```

ADR 通常记录：背景、候选方案、最终决定、原因和后果。

## 15. Bug 处理流程

收到 Bug 后不要立刻猜测修改。应按以下顺序：

```text
确认现象
→ 收集环境和日志
→ 稳定复现
→ 定位所属层级
→ 找到根因
→ 增加回归测试
→ 实施最小修复
→ 全量测试
→ 记录限制或后续工作
```

一个高质量 Bug 报告应包括：

- 期望行为。
- 实际行为。
- 复现步骤。
- 设备和系统版本。
- 相关日志。
- 是否稳定复现。
- 最近是否有相关变更。

## 16. 重构规范

重构的目标是改善内部结构，同时保持外部行为不变。

适合重构的信号：

- 同一逻辑重复多次。
- 模块职责混乱。
- 一个文件承担多个层级职责。
- 修改一个功能总会破坏无关模块。
- 测试难以隔离。

重构前应确保关键行为有测试。大型重构不要与新功能混在同一 PR 中，否则很难审核问题来自哪里。

## 17. Demo 与发布阶段

### 17.1 当前 dev 阶段

当前阶段重点：

- 五层核心链路能够运行。
- 至少形成一个完整任务 Demo。
- 真实设备执行可观测、可调试。
- 核心错误能够解释。
- 自动化测试覆盖关键边界。

当前不做：

- PyPI 发布。
- GitHub Release。
- 稳定 API 承诺。
- 复杂发布自动化。

### 17.2 第一个 Demo 的完成条件

建议至少满足：

- L1 至 L5 的最小链路贯通。
- 能完成一个明确、可复现的 Android 任务。
- 控件树优先，缺失时能进入视觉路径。
- 执行动作有结构化结果。
- 失败能够留下足够调试信息。
- 新电脑可以根据 README 成功安装和运行。
- 所有自动化测试通过。

### 17.3 Demo 后再讨论发布

Demo 稳定后再决定：

- 正式版本号。
- GitHub Release 内容。
- Wheel 和源码包。
- 安装命令。
- Release Notes。
- CI 构建和发布流程。

## 18. Definition of Done

一个任务只有满足以下条件才算完成：

- [ ] 需求和非范围明确。
- [ ] 代码位于正确层级。
- [ ] 公共接口经过考虑。
- [ ] 正常流程已实现。
- [ ] 错误和边界场景已处理。
- [ ] 新增或更新测试。
- [ ] 单元测试通过。
- [ ] 相关真机测试通过。
- [ ] Ruff 检查和格式检查通过。
- [ ] 文档与行为一致。
- [ ] `git diff` 中没有无关修改。
- [ ] 没有提交密钥、本地环境或构建产物。
- [ ] Commit 信息清晰。
- [ ] PR 描述包含验证结果和已知限制。

## 19. 常见错误

### 19.1 长期不提交

风险：电脑故障、切换设备困难、无法回退、改动逐渐失去边界。

### 19.2 一个分支开发所有功能

风险：PR 过大、冲突严重、无法单独合并或回退。

### 19.3 只测试正常流程

风险：真实环境中的超时、离线、空数据和并发问题没有被发现。

### 19.4 为通过测试而修改测试

测试失败时应先判断实现是否错误，不能直接降低断言标准。

### 19.5 捕获所有异常但不处理

下面的代码会隐藏真实问题：

```python
try:
    operation()
except Exception:
    pass
```

应只捕获能够明确处理的异常，并保留足够错误信息。

### 19.6 无限制增加依赖

每个依赖都会带来版本、供应链、兼容性和维护成本。添加依赖前应确认它确实比少量清晰代码更合适。

## 20. 推荐的阶段推进方式

对于 vPhone，建议按照以下节奏推进：

```text
讨论一个层级的职责
→ 冻结最小公共接口
→ 实现最小可运行版本
→ 离线单元测试
→ 真机验证
→ 修复真实问题
→ 审核代码
→ 合并 main
→ 再开始下一层
```

近期可以采用：

```text
feature/l1-device
→ 审核 L1
→ 合并 main
→ 创建 feature/l2-action
→ 开发 L2
```

不要在 L1 尚未审核和合并时，同时大规模修改 L2、L3 和 Planner。逐层形成稳定基础，可以显著降低后期返工。

## 21. 每日检查清单

开始工作：

```text
[ ] git status
[ ] 切换到正确分支
[ ] pull --ff-only 或 fetch
[ ] uv sync --extra dev
[ ] 阅读当前任务和验收条件
```

结束工作：

```text
[ ] pytest 通过
[ ] ruff check 通过
[ ] ruff format --check 通过
[ ] git diff 已审核
[ ] 没有敏感信息和构建产物
[ ] commit 信息清晰
[ ] push 到 GitHub
[ ] 必要时更新 PR 描述
```

## 22. 最重要的习惯

如果只记住几件事，请记住：

1. 开发前先明确目标、范围和验收条件。
2. 从最新的 `main` 创建短生命周期分支。
3. 小步开发、小步提交、小步审核。
4. 公共接口和层级边界比内部写法更重要。
5. 修复 Bug 时寻找根因并增加回归测试。
6. 真实设备功能必须经过真机验证。
7. 离开电脑前提交并推送有价值的进度。
8. 不强推共享 `main`，不提交密钥和本机环境。
9. 冲突不可怕，未经测试的冲突解决才危险。
10. 项目推进的目标不是写最多代码，而是持续形成可验证、可维护的增量。

